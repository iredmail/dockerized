#
# This file is managed by iRedMail Team <support@iredmail.org> with Ansible,
# please do __NOT__ modify it manually.
#

connect = host=iredmail-mariadb port=3306 dbname=vmail user=vmailadmin password=FGxhe4WK5u56vnAqQr9BnZ96yV965xbz

map {
    pattern = shared/last-login/$user/$domain
    table = last_login
    value_field = last_login
    value_type = uint

    fields {
        username = $user
        domain = $domain
    }
}
